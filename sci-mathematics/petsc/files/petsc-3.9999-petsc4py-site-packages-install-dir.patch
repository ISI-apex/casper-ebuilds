--- a/config/BuildSystem/config/packages/petsc4py.py	2021-03-20 01:23:13.055852000 -0000
+++ b/config/BuildSystem/config/packages/petsc4py.py	2021-03-20 01:24:34.660727000 -0000
@@ -31,7 +31,10 @@
 
   def Install(self):
     import os
-    installLibPath = os.path.join(self.installDir, 'lib')
+    import site
+    installLibPath = site.getsitepackages()[0]
+    os.makedirs(installLibPath, exist_ok=True)
+    installLibPath = os.path.realpath(installLibPath)
     if self.setCompilers.isDarwin(self.log):
       apple = 'You may need to\n (csh/tcsh) setenv MACOSX_DEPLOYMENT_TARGET 10.X\n (sh/bash) MACOSX_DEPLOYMENT_TARGET=10.X; export MACOSX_DEPLOYMENT_TARGET\nbefore running make on PETSc'
     else:
